import React from 'react';

import { PageLoader } from '../ui/LoadingComponents';

const LoadingScreen: React.FC = () => <PageLoader message="Preparing your writing space..." />;

export default LoadingScreen;
