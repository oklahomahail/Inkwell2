// src/components/Navigation/PowerToolsMenu.tsx // Updated to resolve TypeScript compilation errors import { Wrench, Search, Clock, Users, GitBranch, FileText, Upload, Download, BarChart, Settings, Zap, Map, } from 'lucide-react'; import React, { useState, useRef } from 'react'; import { analyticsService } from '../../services/analyticsService'; import { // featureFlagService (removed) } from '../../services/// featureFlagService (removed)'; import { Badge } from '../ui/badge'; import { Button } from '../ui/Button'; import { Input } from '../ui/Input'; import { Popover, PopoverContent, PopoverTrigger } from '../ui/popover'; interface PowerTool { id: string; name: string; description: string; icon: React.ComponentType<any>; category: 'structure' | 'analysis' | 'collaboration' | 'data' | 'advanced'; keywords: string[]; isNew?: boolean; isPro?: boolean; onClick: () => void; } interface PowerToolsMenuProps { projectId?: string; onToolSelect?: (toolId: string) => void; } export function PowerToolsMenu({ projectId, onToolSelect }: PowerToolsMenuProps) { const [isOpen, setIsOpen] = useState(false); const [searchQuery, setSearchQuery] = useState(''); const searchInputRef = useRef<HTMLInputElement>(null); // Check if power tools menu should be shown const showPowerMenu = true; // Always show power menu