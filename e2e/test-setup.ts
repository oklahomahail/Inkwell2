// This file provides types and configurations for Playwright tests
// in a format that doesn't interfere with the main project's test setup

// Re-export the Playwright test API
export { test, expect } from '@playwright/test';

// Add custom matchers or utilities here if needed
